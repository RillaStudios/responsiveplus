publish_to: none

name: responsive
description: A Flutter package to make responsive design easy and customizable. This package is inspired by multiple Flutter packages and aims to provide a combination of all of them in one. Inspired by flutter_screenutil, sizer, and auto_size_text.
version: 0.0.1

environment:
  sdk: ">=3.4.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  web: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# The following section is specific to Flutter packages.
flutter:
